RewriteEngine On
RewriteBase /

# Serve the app for direct route access
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]

# Optional: keep clean URLs working on shared hosting
Options -MultiViews
